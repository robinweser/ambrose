import core from './core'

const { colors } = core

export default {
  colors: {
    foreground: {
      primary: colors.black,
      alert: colors.red900,
    },
    background: {
      primary: colors.grey100,
    },
  },
}
