import core from './core'

const { colors } = core

export default {
  colors: {
    foreground: {
      primary: colors.white,
      alert: colors.red100,
    },
    background: {
      primary: colors.grey900,
    },
  },
}
